<!DOCTYPE html>
%html
  %head
    %meta{"http-equiv"=>"Content-type", :content=>"text/html;charset=utf-8"}/
    %title Active Blogr
    = stylesheet_link_tag 'application', 'css1', 'css2'
    = stylesheet_link_tag 'http://fonts.googleapis.com/css?family=Droid+Serif:regular,italic,bold,bolditalic&subset=latin'
    = javascript_include_tag 'jquery', "http://platform.twitter.com/widgets.js"
  %body
    #content
      #header
        #logo
          %a{:href => root_path, :title=>"Home"}
            = image_tag 'logo.gif'
        - flash.each do |key, value|
          .messages{:id => "flash_#{key}"}
            - if value.respond_to?(:each) 
              %ul
              - value.each do |error|
                %li
                  = error
            - else
              = value 
        

        = yield
      #footer
        #footer-content
          %p
            = link_to 'Contact', 'mailto:activeblogr@simonmathieu.com'
            \-
            = link_to 'About me', 'http://simonmathieu.com'


